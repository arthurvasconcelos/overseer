---
title: update
weight: 17
---

Check for and apply a new overseer release.

```bash
overseer update
```

Fetches the latest release from GitHub, compares it against the running version, and replaces the binary if a newer version is available.

If you installed via Homebrew, prefer `brew upgrade arthurvasconcelos/tap/overseer` instead.
